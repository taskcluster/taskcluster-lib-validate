version: 0
metadata:
  name: Generate taskcluster-lib-validate Docs
  description: Generate and upload docs for this library.
  owner: "{{ event.head.user.email }}"
  source: "{{ event.head.repo.url }}"
tasks:
  - provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    extra:
      github:
        env: true
        events:
          - push
        # TODO: Enable the following
        # branches:
        #   - master
    payload:
      maxRunTime: 3600
      image: "quay.io/mrrrgn/ubuntu-ci"
      artifacts:
        public:
          type: directory
          path: "/home/worker/repo/docs/taskcluster-lib-validate/"
          expires: "2016-04-18T17:21:34.766Z"
      command:
        - "/bin/bash"
        - "-lc"
        - "checkout-pull-request && npm install . && npm run docs"
    metadata:
      name: Generate taskcluster-lib-validate Docs
      description: Generate and upload docs for this library.
      owner: "{{ event.head.user.email }}"
      source: "{{ event.head.repo.url }}"

