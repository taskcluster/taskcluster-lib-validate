version: 0
metadata:
  name: Generate taskcluster-lib-validate Docs
  description: Generate and upload docs for this library.
  owner: "{{ event.head.user.email }}"
  source: "{{ event.head.repo.url }}"
tasks:
  - provisionerId: "{{ taskcluster.docker.provisionerId }}"
    workerType: "{{ taskcluster.docker.workerType }}"
    extra:
      github:
        env: true
        events:
          - push
        # TODO: Enable the following
        # branches:
        #   - master
    routes:
      - index.garbage.bstack.docs.taskcluster-lib-validate
    payload:
      maxRunTime: 3600
      image: "node:4.4.0"
      artifacts:
        public:
          type: directory
          path: "/repo/docs/taskcluster-lib-validate/"
          expires: "2016-04-18T17:21:34.766Z"
      command:
        - "/bin/bash"
        - "-lc"
        - "git clone $GITHUB_HEAD_REPO_URL repo && cd repo && git checkout $GITHUB_HEAD_BRANCH && npm install . && npm run docs"
    metadata:
      name: Generate taskcluster-lib-validate Docs
      description: Generate and upload docs for this library.
      owner: "{{ event.head.user.email }}"
      source: "{{ event.head.repo.url }}"

